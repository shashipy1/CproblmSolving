#include<stdio.h>
int main(){
    int *p;
    float *fp;
    char *cp;
    double *dp;
    p = cp + fp;// invalic operand
    dp = cp / fp;
    cp = p * fp;
    dp = dp - cp;
*dp = *P*fp/*cp-((*fp - * *P) *dp);
p++;
dp++;
fp--;
cp--;
p = p + 7;
fp fp - 3;
dp = dp - 3;
cp = cp + 4;
dp = dp*3;
fp = fp / 4;
}